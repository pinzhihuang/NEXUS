# Nixpacks configuration for Railway deployment
# Adds Chromium to the standard Python environment

[phases.setup]
nixPkgs = ["...", "chromium"]
aptPkgs = ["chromium", "chromium-driver"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-8000} --workers 4 --threads 2 --timeout 120"
